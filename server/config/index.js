'use strict';

module.exports = {
    // serviceKey: path.join(global.__basedir, process.env.GOOGLE_APPLICATION_CREDENTIALS),
    // projectId: process.env.PROJECT_ID,
    serviceKey: process.env.GOOGLE_APPLICATION_CREDENTIALS,
    // projectId: "qp-qbot-09-2020",
    // serviceAccountEmail: "df-firestore-sa@qp-qbot-2020-09.iam.gserviceaccount.com",
    // google_scopes: ["https://www.googleapis.com/auth/cloud-platform"],
}
